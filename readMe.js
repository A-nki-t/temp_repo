// GLOBAL (veriables) - no window !!

// __dirname   - path of the current directory
// __filename  - file name
// require     - function to use current module ( file)
// module      - info about current module (file)
// process     - info about env where the program is being executed

//````````````````````````````````````````````````````````

// CommonJS, every file is a module (by default)
// Modules - Encapsulated Code (only share minimum)



//// built in modules 
// OS
// PATH 
// FS  (FILE SYSTEM)
// HTTP 





// local dependency - use it only in this project
//npm i <packageName>

//global dependency - use it in any project
//npm install -g <packageName>
// sudo npm install -g <packageName> (mac )

//package.json - manifest file(stores important info about project/package)
//manual approach (create package.json in the root, creat properties etc)
//npm init( step by step, press enter to skip)
// npm init -y (everything default)

//installing packages will add dependency in the installed_modeules folder
//
//some modules have a single dependency while others have more